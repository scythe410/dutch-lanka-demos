# Customer app — runtime config loaded via flutter_dotenv.
# Copy to `.env` (gitignored) and fill in real values for local dev.

# Google Maps SDK key. Restrict to Android/iOS bundle IDs in the Google Cloud
# Console — never reuse the unrestricted browser key here.
GOOGLE_MAPS_API_KEY=
