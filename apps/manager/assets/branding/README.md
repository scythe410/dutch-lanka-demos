# Branding assets

Same notes as `apps/customer/assets/branding/README.md`. The manager app
uses its own icon so the home-screen distinguishes the staff console from
the customer app at a glance.

`app_icon.png` (1024×1024) is the source. After replacing the placeholder:

```
flutter pub run flutter_launcher_icons
```
